{
    "name": "klynaa-blockchain",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "hardhat node",
        "build": "echo 'compile handled by hardhat'",
        "test": "hardhat test",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "hardhat": "^2.22.5",
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "prettier": "^3.2.5"
    },
    "dependencies": {
        "ethers": "^6.10.0"
    }
}